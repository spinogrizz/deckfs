# Configuration file for deckfs

# Stream Deck display brightness (0-100)
# Default: 50
brightness: 50

# Debounce interval for button presses in seconds
# Prevents accidental double-clicks and file system event flooding
# Default: 0.1
debounce_interval: 0.1